## Build

### .env

```properties
POSTGRES_USER=username
POSTGRES_PASSWORD=password
POSTGRES_DB=name
POSTGRES_HOST=db

DB_PORT=5432

DATABASE_URL="postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}/${POSTGRES_DB}"

NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=""

SCRAPPER_INTERVAL_MIN=15
```
